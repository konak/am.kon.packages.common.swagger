using System;
namespace am.kon.packages.common.swagger
{
    /// <summary>
    /// Class for constants used internally in the package.
    /// </summary>
    internal static class InternalConstants
    {
        internal const string BearerSchemeUsageDescription = @"JWT Authorization header using the Bearer scheme. \r\n\r\n 
                      Enter 'Bearer' [space] and then your token in the text input below.
                      \r\n\r\nExample: 'Bearer 12345abcdef'";

        internal const string AuthorizationHeadeerName = "Authorization";
    }
}

